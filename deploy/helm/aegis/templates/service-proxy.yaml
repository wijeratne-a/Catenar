apiVersion: v1
kind: Service
metadata:
  name: aegis-proxy
spec:
  selector:
    app: aegis-proxy
  ports:
    - name: http
      port: {{ .Values.proxy.port }}
      targetPort: {{ .Values.proxy.port }}

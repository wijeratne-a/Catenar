[package]
name = "catenar-verify"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
serde_json = "1"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
